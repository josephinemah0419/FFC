import { localizedMetadata } from "@/locales/metadata";
export const metadata = localizedMetadata("about", "en", "/about");
export default function AboutLayout({ children }: { children: React.ReactNode }) { return children; }
