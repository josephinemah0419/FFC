import { localizedMetadata } from "@/locales/metadata";
export const metadata = localizedMetadata("certification", "en", "/certification");
export default function CertificationLayout({ children }: { children: React.ReactNode }) { return children; }
